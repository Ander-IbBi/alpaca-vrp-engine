# Runtime deps for Streamlit Community Cloud (pip, not uv).
# Keep in sync with the [project.dependencies] list in pyproject.toml.
alpaca-py>=0.44.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
streamlit>=1.40.0
pandas>=2.2.0
