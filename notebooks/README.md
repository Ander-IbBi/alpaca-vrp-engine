# Notebooks

Research space: option chains, greeks, payoff plots, strike selection experiments.

Nothing here is the deliverable. The submission is `src/options_agent/` plus the
Streamlit demo. When an idea works, port it into `strategy/` with a unit test.

Notebook conventions (see `.cursor/rules/coding-style.mdc`): English, a title cell,
`##` sections, and a short markdown explanation before each non-trivial code cell.
